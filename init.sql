-- init.sql: Sample seed data loaded on first startup
-- Tables are created by Spring Boot (JPA ddl-auto=update)
-- This runs AFTER tables exist only if you run it manually or via migration tool.
-- For now this file is a placeholder — Spring Boot auto-creates the schema.

-- You can manually run after the backend starts:
-- INSERT INTO users (name, created_at) VALUES ('Raj', NOW()), ('Priya', NOW()), ('Dev', NOW());
